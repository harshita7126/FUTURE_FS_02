# FUTURE_FS_02

Minimal advanced CRM platform implementation with:

- Authentication (password hashing + session tokens)
- User-scoped workspaces and member authorization
- Lead management and sales pipeline stage updates
- Task assignment and completion tracking
- Analytics dashboard aggregates per workspace
- Optional Supabase synchronization hooks

## Run tests

```bash
python -m unittest discover -s tests -v
```
